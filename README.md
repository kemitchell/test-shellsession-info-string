This text appears before a Markdown fenced code block with the info string "shell".

```shell
echo Hello!
```

This text appears before a Markdown fenced code block with the info string "shellsession".

```shell
echo Hello again!
```
